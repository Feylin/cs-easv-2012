package sco.collaborative.zuul;

public class MainClass {

	public static void main( String[] args ){

		/*
		 * This is just some text so that the MainClass.java changes, 
		 * and I can push it to a new branch and eventually merge it 
		 * into origin/master instead of pushing it directly there.
		 */
		new Game().play();

	}
}
